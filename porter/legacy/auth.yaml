version: v2
name: qm-auth
build:
  method: docker
  context: .
  dockerfile: ./deploy/auth/Dockerfile
services:
  - name: auth
    type: web
    private: true
    run: node src/index.ts
    instances: 1
    cpuCores: 0.25
    ramMegabytes: 512
    port: 8080
